//  ENSURE QUESTION     KYU 8
function ensureQuestion(s) {
  return s.endsWith("?") ? s : s + "?";
}

//  All Star Code Challenge #18     KYU 8
function strCount(str, letter) {
  //code here
}

//Age Range Compatibility Equation  KYU 8
//  Parmeters:  given age (an integer between 1 and 100)
//  Return: min and max age range
// if <= 14 diff formula
//  Example:
//  PseudoCode:
// check if age is over 14
// if so, do the regular formula and return string template
// otherwise return other formula string temp.

function datingRange(age) {
  const min = age / 2 + 7;
  const max = (age - 7) / 2;
}
